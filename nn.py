import numpy as np

INPUT_SIZE = 16
OUTPUR_SIZE = 10


class fully_connected_layer(self, layer_size):
    def __init__(self):
        self.weights = 


input_layer = np.array(IMPUT_SIZE)


output_layer = np.array(OUTPUT_SIZE)

